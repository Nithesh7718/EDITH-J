# Local override file for non-secret settings only.
# Do not store API keys or other secrets here.
#
# Example overrides:
# app.name=EDITH-J
# groq.model=llama-3.3-70b-versatile
# groq.base-url=https://api.groq.com/openai/v1
# storage.backend=sqlite
# storage.db-path=C:/Users/your-user/.edith-j/data/edith.db

# Optional: prefer a specific microphone by exact device name.
speech.audio.input-device-name=Microphone Array (AMD Audio Device)

# Optional: map WhatsApp recipient names to phone numbers for direct chat opens.
# Example:
# edith.whatsapp.contact.krithick=+919876543210

# Optional: World Monitor base URL override (API key should stay in env var WORLD_MONITOR_API_KEY).
# worldmonitor.base-url=https://www.worldmonitor.app
